--Q21. Create a CTE that computes total income, then filter only those with total income > 10,000.

Q22. Create a CTE that ranks applicants by LoanAmount and returns only the top 50.
Q23. Create a multi-CTE pipeline:
- CTE1 ? compute total income
- CTE2 ? categorize income
- CTE3 ? compute average loan amount per category
Q24. Identify outliers in LoanAmount using window functions (e.g., > mean + 2*stddev).
Q25. Find the highest loan amount for each combination of Education + Property_Area.